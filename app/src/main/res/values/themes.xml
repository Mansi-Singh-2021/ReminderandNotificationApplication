<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ReminderAndNotificationApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Dark theme colors -->
        <item name="android:windowBackground">@color/dark_bg</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/teal_accent</item>
    </style>

    <style name="Theme.ReminderAndNotificationApplication" parent="Base.Theme.ReminderAndNotificationApplication" />
</resources>